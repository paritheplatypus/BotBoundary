# Production (NO purchased domain):
# Use the API Gateway execute-api URL (HTTP API):
#   VITE_API_BASE_URL=https://<api-id>.execute-api.<region>.amazonaws.com

# Auth transport:
#   bearer (recommended for execute-api URLs)
#   cookie  (only if API is same-site)
#   both
VITE_AUTH_MODE=bearer

# Local dev uses the Vite proxy (/api -> http://127.0.0.1:8000), so you can omit VITE_API_BASE_URL.
