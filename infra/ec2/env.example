# Shared environment file for both services (/opt/botboundary/.env)

ENVIRONMENT=production

# DynamoDB
AWS_REGION=us-east-1
USERS_TABLE_NAME=Users
SESSIONS_TABLE_NAME=Sessions
BEHAVIORAL_EVENTS_TABLE_NAME=BehavioralEvents

# API
CORS_ORIGINS=https://YOUR_AMPLIFY_DOMAIN_HERE
JWT_SECRET=CHANGE_ME_LONG_RANDOM
JWT_TTL_MINUTES=60
JWT_COOKIE_NAME=bb_auth

# Model service
MODEL_URL=http://127.0.0.1:8001
MODEL_API_KEY=CHANGE_ME_INTERNAL
FEATURE_DIM=28

# Risk thresholds
ALLOW_THRESHOLD=0.80
CHALLENGE_THRESHOLD=0.95
